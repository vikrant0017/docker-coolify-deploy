name: todo-web

services:
  web:
    build: .
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:4000}
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - devnet

networks:
  devnet:
    external: true
