# Coolify environment variables for the web resource.
# The API service receives the todo-api alias from docker-compose.coolify.yml
# on the external coolify network.
API_UPSTREAM=http://todo-api:4000
