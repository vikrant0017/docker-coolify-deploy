# Coolify environment variables for the auth resource.
# Replace this with a strong, unique password before pasting it into Coolify.
# Keep it private: it initializes Postgres and is also used by the auth service.
AUTH_DB_PASSWORD=replace-with-a-long-unique-password
